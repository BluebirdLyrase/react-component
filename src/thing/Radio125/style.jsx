import styled from 'styled-components';

export const MinMaxPoint = styled.span`
  margin: auto;
`;

export const RadioContainer = styled.div`
  position: relative;
  display: flex;
  justify-content: space-between;
`;
